@page
@model DemoPRN1.Pages.Admin.Categories.UpdateCategoriesModel
@{
    Layout = "/Pages/Shared/_LayoutAdmin.cshtml";
}

                <!-- End of Topbar -->
                <div class="container-fluid">
                    <h2>Thêm thể loại sách</h2>

                    <form method="post" >
                        <div class="form-group">
                            <label asp-for="updateCategory.CategoryId"></label>
                            <input type="text" readonly="true" asp-for="updateCategory.CategoryId" class="form-control"/>
                            <span asp-validation-for="updateCategory.CategoryId" class="text-danger"></span>
                        </div>
                      
                        <div class="form-group">
                            <label asp-for="updateCategory.CategoryName"></label>
                            <input asp-for="updateCategory.CategoryName" class="form-control" />
                            <span asp-validation-for="updateCategory.CategoryName" class="text-danger"></span>
                        </div>
                        <button type="submit" class="btn btn-primary">Sửa đổi</button>
                        @if (!ModelState.IsValid)
                        {
                            <div class="alert alert-danger">
                                @foreach (var error in ModelState.Values.SelectMany(v => v.Errors))
                                {
                                    <p>@error.ErrorMessage</p>
                                }
                            </div>
                        }
                     </form>
                </div>

             

